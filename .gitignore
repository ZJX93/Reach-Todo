# 依赖
node_modules/
.pnp/
.pnp.js

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# 本地数据库（运行时生成）
*.db
*.sqlite3

# 密钥 / 环境变量
.env
.env.local
.jwt_secret

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/

# 内部 / 代理
.codebuddy/

# 日志
*.log

# 编辑器 / 环境配置
.cloudstudio

# Vite 构建临时缓存（build 时生成的 vite.config 时间戳副本）
frontend/vite.config.*.timestamp-*.mjs

# 前端构建产物：vite 输出到 backend/static，由 Docker 多阶段构建在容器内生成，
# 不属于源码，不应提交（避免哈希化资源在每次构建后产生大量 diff）。
backend/static/
